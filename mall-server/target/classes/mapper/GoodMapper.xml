<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rabbiter.em.mapper.GoodMapper">
    <insert id="insertGood" useGeneratedKeys="true" keyProperty="id">
        insert into good(name, description, discount, category_id, imgs, create_time) value (#{good.name},#{good.description},#{good.discount},#{good.categoryId},#{good.imgs},#{good.createTime})
    </insert>
    <update id="saleGood">
        update `good` set sales = sales + #{count} ,sale_money = sale_money + #{money} where good.id = #{id}
    </update>


    <select id="findFrontGoods" resultType="com.rabbiter.em.entity.dto.GoodDTO">
        SELECT good.*,MIN(good_standard.price)*discount as price FROM `good` LEFT JOIN good_standard on good.id = good_standard.good_id  WHERE is_delete = 0 AND status = 1 AND recommend = 1 GROUP BY id  ORDER BY price ASC
    </select>

    <select id="getMinPricesByIds" resultType="java.util.Map">
        SELECT gs.good_id as goodId, good.discount * MIN(gs.price) as minPrice
        FROM good_standard gs
        JOIN good ON good.id = gs.good_id
        WHERE gs.good_id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
        GROUP BY gs.good_id
    </select>

    <select id="getTotalStoreByIds" resultType="java.util.Map">
        SELECT good_id as goodId, IFNULL(SUM(store), 0) as totalStore
        FROM good_standard
        WHERE good_id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
        GROUP BY good_id
    </select>
</mapper>
