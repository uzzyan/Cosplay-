# U次元 Cosplay服装商城系统 - 测试报告

## 1. 测试概述

| 项目 | 内容 |
| :--- | :--- |
| **项目名称** | U次元 - Cosplay服装商城系统 |
| **测试日期** | 2026-04-12 |
| **测试环境** | JDK 1.8、Spring Boot 2.5.6、Vue.js 2.6.14、MySQL 5.7、Redis |
| **测试框架** | JUnit 5 (Jupiter)、Mockito 3.x、Spring MockMvc、JaCoCo |
| **测试执行** | Maven Surefire 2.22.2 |

### 1.1 测试范围

本次测试覆盖系统以下核心模块：

- 用户认证模块（注册、登录、密码强度验证）
- 商品管理模块（商品查询、推荐系统）
- 订单管理模块（下单、支付、确认收货）
- 推荐算法模块（协同过滤、冷启动兜底）
- 安全性验证（密码规则、权限控制）

---

## 2. 单元测试执行结果

### 2.1 测试执行汇总

| 指标 | 数值 |
| :--- | :--- |
| **测试用例总数** | 16 |
| **通过** | 16 |
| **失败** | 0 |
| **错误** | 0 |
| **跳过** | 0 |
| **通过率** | 100% |
| **总耗时** | 5.694s |
| **分析类数** | 75 classes (JaCoCo) |

### 2.2 各测试类执行详情

#### 2.2.1 GoodControllerTest（商品控制器测试）

| 用例编号 | 测试方法 | 测试目的 | 结果 | 耗时 |
| :--- | :--- | :--- | :--- | :--- |
| UT-001 | testFindAll_ReturnsFrontGoods | 验证商品列表接口返回正确的JSON结构 | 通过 | 4.197s |
| UT-002 | testRecommend_ReturnsRecommendedGoods | 验证推荐接口返回推荐商品数据 | 通过 | (含上) |

**测试策略**：使用 MockMvc 模拟 HTTP 请求，Mock GoodService 和 RecommendationService，验证接口返回的 HTTP 状态码和 JSON 响应结构。

#### 2.2.2 OrderServiceTest（订单服务测试）

| 用例编号 | 测试方法 | 测试目的 | 结果 | 耗时 |
| :--- | :--- | :--- | :--- | :--- |
| UT-003 | testReceiveOrder | 验证确认收货调用了正确的 Mapper 方法 | 通过 | 1.154s |
| UT-004 | testSaveOrder | 验证创建订单流程：插入订单、关联商品、清除购物车 | 通过 | (含上) |
| UT-005 | testPayOrder | 验证支付流程：状态变更、库存扣减、销量更新、Redis缓存 | 通过 | (含上) |

**测试策略**：Mock OrderMapper、StandardMapper、GoodMapper、CartService、RedisTemplate，使用 MockedStatic 模拟 TokenUtils 静态方法。通过 ReflectionTestUtils 注入 MyBatis-Plus 的 baseMapper 依赖。

#### 2.2.3 RecommendationServiceTest（推荐算法测试）

| 用例编号 | 测试方法 | 测试目的 | 结果 | 耗时 |
| :--- | :--- | :--- | :--- | :--- |
| UT-006 | testRecommend_WithNoInteractions | 验证无交互记录时的冷启动兜底策略（按销量排序） | 通过 | 0.002s |
| UT-007 | testRecommend_WithInteractions | 验证有交互记录时的推荐降级逻辑 | 通过 | (含上) |

**测试策略**：Mock OrderGoodsMapper 和 GoodMapper，验证推荐算法在不同数据条件下的行为。

#### 2.2.4 UserServiceTest（用户服务测试）

| 用例编号 | 测试方法 | 测试目的 | 结果 | 耗时 |
| :--- | :--- | :--- | :--- | :--- |
| UT-008 | testLogin_Success | 验证正确凭据登录成功，返回Token并缓存到Redis | 通过 | 0.341s |
| UT-009 | testLogin_Fail_WrongPassword | 验证用户名不存在时抛出异常 | 通过 | (含上) |
| UT-010 | testRegister_Success | 验证使用符合规则的密码(Abc123!@)注册成功 | 通过 | (含上) |
| UT-011 | testRegister_Fail_UserExists | 验证重复用户名注册时抛出异常 | 通过 | (含上) |
| UT-012 | testRegister_Fail_WeakPassword_NoSpecialChar | 验证缺少特殊符号的密码被拒绝 | 通过 | (含上) |
| UT-013 | testRegister_Fail_WeakPassword_NoDigit | 验证缺少数字的密码被拒绝 | 通过 | (含上) |
| UT-014 | testRegister_Fail_WeakPassword_NoLetter | 验证缺少字母的密码被拒绝 | 通过 | (含上) |
| UT-015 | testRegister_Fail_WeakPassword_TooShort | 验证过短密码(<6位)被拒绝 | 通过 | (含上) |
| UT-016 | testRegister_Fail_WeakPassword_TooLong | 验证过长密码(>12位)被拒绝 | 通过 | (含上) |

**测试策略**：Mock UserMapper 和 RedisTemplate，使用 MockedStatic 模拟 TokenUtils。新增 5 个密码强度验证用例覆盖所有边界条件。

---

## 3. 功能测试用例（手工测试）

### 3.1 用户登录与 RBAC 权限测试

测试目的：验证系统是否能正确识别管理员与用户身份，并执行权限隔离。

前置条件：数据库中存在管理员账号 admin 和用户账号 user。

| 用例编号 | 测试操作步骤 | 预期结果 | 实际结果 | 结论 |
| :--- | :--- | :--- | :--- | :--- |
| TC-001 | 输入未注册的用户名登录 | 提示"用户名或密码错误" | 提示"用户名或密码错误" | 通过 |
| TC-002 | 输入正确用户名，错误密码 | 提示"用户名或密码错误" | 提示"用户名或密码错误" | 通过 |
| TC-003 | 使用用户账号 user 登录 | 成功登录，跳转至商城首页 | 跳转商城首页 | 通过 |
| TC-004 | 使用管理员账号 admin 登录 | 成功登录，跳转管理员后台 | 跳转至管理员后台 | 通过 |

### 3.2 用户注册密码强度测试

测试目的：验证新的密码规则（6-12位，含字母+数字+特殊符号）在注册流程中的执行效果。

| 用例编号 | 测试操作步骤 | 预期结果 | 实际结果 | 结论 |
| :--- | :--- | :--- | :--- | :--- |
| TC-005 | 注册时输入密码 "123456" | 前端提示密码格式不符合要求 | 前端拦截，提示密码规则 | 通过 |
| TC-006 | 注册时输入密码 "abcdef" | 前端提示密码格式不符合要求 | 前端拦截，提示密码规则 | 通过 |
| TC-007 | 注册时输入密码 "Abc12!" | 注册成功 | 注册成功，跳转登录页 | 通过 |
| TC-008 | 注册时输入密码 "Ab1!" (4位) | 前端提示密码格式不符合要求 | 前端拦截，提示密码规则 | 通过 |
| TC-009 | 注册时输入密码 "Abcdefg12345!" (13位) | 前端提示密码格式不符合要求 | 前端拦截，提示密码规则 | 通过 |

### 3.3 商品浏览与搜索功能测试

| 用例编号 | 测试操作步骤 | 预期结果 | 实际结果 | 结论 |
| :--- | :--- | :--- | :--- | :--- |
| TC-010 | 访问商城首页 | 显示轮播图和推荐商品 | 首页正常渲染 | 通过 |
| TC-011 | 搜索关键词"海贼王" | 列表展示包含关键词的商品 | 商品列表正确筛选 | 通过 |
| TC-012 | 点击商品进入详情页 | 显示商品信息、规格选择、价格 | 详情页正确渲染 | 通过 |

### 3.4 购物车与订单流程测试

| 用例编号 | 测试操作步骤 | 预期结果 | 实际结果 | 结论 |
| :--- | :--- | :--- | :--- | :--- |
| TC-013 | 选择规格后点击"加入购物车" | 提示添加成功 | 添加成功 | 通过 |
| TC-014 | 商品详情页点击"购买"并提交订单 | 订单创建成功，状态为待付款 | 订单创建成功 | 通过 |
| TC-015 | 待付款订单点击"支付" | 订单状态变更为"已支付" | 状态正确变更 | 通过 |
| TC-016 | 管理员对已支付订单点击"发货" | 订单状态变更为"已发货" | 状态正确变更 | 通过 |
| TC-017 | 用户对已发货订单点击"确认收货" | 订单状态变更为"已收货" | 状态正确变更 | 通过 |

### 3.5 地址管理测试

| 用例编号 | 测试操作步骤 | 预期结果 | 实际结果 | 结论 |
| :--- | :--- | :--- | :--- | :--- |
| TC-018 | 新增地址，手机号输入非法格式 | 前端提示格式错误 | 前端拦截提示 | 通过 |
| TC-019 | 新增地址，输入完整合法信息 | 保存成功，列表刷新 | 保存成功 | 通过 |

### 3.6 文件上传安全性测试

| 用例编号 | 测试操作步骤 | 预期结果 | 实际结果 | 结论 |
| :--- | :--- | :--- | :--- | :--- |
| TC-020 | 上传超过30MB的文件 | 后端拒绝，提示文件过大 | 被 Spring 限制拦截 | 通过 |
| TC-021 | 上传正常图片文件(jpg/png) | 上传成功，返回文件URL | 上传成功 | 通过 |

### 3.7 权限验证测试

| 用例编号 | 测试操作步骤 | 预期结果 | 实际结果 | 结论 |
| :--- | :--- | :--- | :--- | :--- |
| TC-022 | 未登录状态直接访问管理接口 | 被拦截，重定向到登录 | 请求被拦截 | 通过 |
| TC-023 | 普通用户访问 /user/page 管理接口 | 无权限，被 @Authority 拦截 | 权限拦截生效 | 通过 |
| TC-024 | 管理员访问 /user/page 管理接口 | 正常返回用户分页数据 | 数据正常返回 | 通过 |

---

## 4. 安全性测试

### 4.1 密码安全

| 测试项 | 验证内容 | 结果 |
| :--- | :--- | :--- |
| 密码存储 | 新注册密码使用 BCrypt 加密存储 | 通过 |
| 密码规则 | 前后端双重验证：6-12位，必须含字母+数字+特殊符号 | 通过 |
| 透明升级 | 旧 MD5 密码首次登录自动升级为 BCrypt | 通过 |
| 密码传输 | 前端登录时对密码进行 MD5 处理后传输 | 通过 |

### 4.2 认证与授权

| 测试项 | 验证内容 | 结果 |
| :--- | :--- | :--- |
| JWT Token | 登录成功生成 Token，存入 Redis 并设置过期时间 | 通过 |
| 权限注解 | @Authority 注解正确拦截管理端接口 | 通过 |
| 跨域配置 | @CrossOrigin 注解和 CorsConfig 正确配置 | 通过 |

### 4.3 输入验证

| 测试项 | 验证内容 | 结果 |
| :--- | :--- | :--- |
| 注册表单 | 用户名、密码、联系方式非空校验 | 通过 |
| 联系方式格式 | 正则校验：/^[0-9\-+() ]{7,20}$/ | 通过 |
| 文件上传限制 | Spring multipart 限制 30MB 上限 | 通过 |

---

## 5. 性能测试评估

### 5.1 后端启动性能

| 指标 | 数值 |
| :--- | :--- |
| Spring Boot 启动时间 | ~6.6s |
| Tomcat 端口 | 9191 |
| JVM 总启动时间 | ~7.4s |

### 5.2 单元测试执行性能

| 测试类 | 用例数 | 耗时 |
| :--- | :--- | :--- |
| GoodControllerTest | 2 | 4.197s |
| OrderServiceTest | 3 | 1.154s |
| RecommendationServiceTest | 2 | 0.002s |
| UserServiceTest | 9 | 0.341s |
| **总计** | **16** | **5.694s** |

### 5.3 兼容性评估

| 环境 | 状态 | 说明 |
| :--- | :--- | :--- |
| MySQL 5.7 | 兼容 | SQL 语法已适配（utf8mb3 -> utf8） |
| MySQL 8.0 | 兼容 | 原始环境，完全支持 |
| JDK 1.8 | 兼容 | 项目指定 Java 版本 |
| IntelliJ IDEA | 兼容 | 标准 Maven 项目，可直接导入运行 |

---

## 6. 测试文件清单

### 6.1 现有测试文件

| 文件路径 | 用例数 | 覆盖模块 |
| :--- | :--- | :--- |
| `mall-server/src/test/.../controller/GoodControllerTest.java` | 2 | 商品接口层 |
| `mall-server/src/test/.../service/OrderServiceTest.java` | 3 | 订单核心业务 |
| `mall-server/src/test/.../service/UserServiceTest.java` | 9 | 用户认证+密码验证 |
| `mall-server/src/test/.../service/RecommendationServiceTest.java` | 2 | 推荐算法 |

### 6.2 本次新增/修改的测试

| 变更 | 文件 | 说明 |
| :--- | :--- | :--- |
| 修改 | UserServiceTest.java | 注册成功用例密码改为符合新规则的 "Abc123!@" |
| 新增 | UserServiceTest.java | 新增 5 个密码强度验证用例（UT-012 ~ UT-016） |

---

## 7. 发现的问题与修复建议

### 7.1 已修复问题

| 编号 | 问题描述 | 修复状态 |
| :--- | :--- | :--- |
| BUG-001 | Person.vue 重置密码使用 GET 请求，与后端 @PostMapping 不匹配 | 已修复，改为 POST |
| BUG-002 | 管理员新增用户默认密码 "123456" 不符合新密码规则 | 已修复，改为手动输入 |
| BUG-003 | UserServiceTest 注册测试使用弱密码 "password"，与新规则冲突 | 已修复 |

### 7.2 建议改进项

| 编号 | 建议 | 优先级 |
| :--- | :--- | :--- |
| SUG-001 | 后端启动日志有 ReflectLambdaMeta 反射警告，建议升级 MyBatis-Plus 版本 | Low |
| SUG-002 | 建议增加 Controller 层的集成测试覆盖（UserController、OrderController） | Medium |
| SUG-003 | 建议增加前端自动化测试（Vue Test Utils） | Medium |
| SUG-004 | 建议配置 CI/CD 流水线自动执行 `mvn test` | Medium |
| SUG-005 | 建议将文件上传增加文件类型白名单校验（仅允许 jpg/png/gif） | High |

---

## 8. 测试结论

本次测试对 U次元 Cosplay 服装商城系统进行了全面的质量验证：

- **单元测试**：16 个用例全部通过（100% 通过率），覆盖用户认证、商品查询、订单流程、推荐算法四大核心模块
- **密码安全**：新增密码强度规则（6-12位，含字母+数字+特殊符号）在前后端均已生效，5 个边界用例全部验证通过
- **业务流程**：登录/注册、商品浏览/搜索、订单创建/支付/发货/收货 完整链路验证通过
- **权限控制**：@Authority 注解正确拦截管理端接口，JWT Token 认证机制工作正常
- **兼容性**：MySQL 5.7 适配完成，IntelliJ IDEA 可直接导入运行

系统核心功能稳定，安全防护机制有效，可满足正常使用需求。
